---
description: 
globs: 
alwaysApply: true
---
# V3 guides
- New entities are added to this repository under the `entities` folder, followed by the `domain` and then the actual entity. For example: `/entities/access_control/permission.json`
- New swagger are added to this repository under the `swagger` folder, followed by the `domain` and then the actual entity. For example: `/swagger/access_control/permission.json`