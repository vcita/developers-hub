// GENERATED by scripts/generate-zapier.js — do not edit by hand.
const { BASE_URL } = require('../constants');
const { getByPath } = require('../utils');

const PATH = "/platform/v1/clients";
const ARRAY_PATH = "data.clients";
const ID_FIELD = "id";
const LABEL_FIELDS = [
  "first_name",
  "last_name"
];
const QUERY = {};

const perform = async (z, bundle) => {
  const response = await z.request({ url: `${BASE_URL}${PATH}`, method: 'GET', params: QUERY });
  const list = getByPath(response.data, ARRAY_PATH) || [];
  return list.map((item) => ({
    ...item,
    id: item[ID_FIELD],
    label: LABEL_FIELDS.map((f) => item[f]).filter(Boolean).join(' ') || String(item[ID_FIELD]),
  }));
};

module.exports = {
  key: "list_clients",
  noun: "Client",
  display: {
    label: "List Clients",
    description: "Internal: lists clients to populate dynamic dropdowns.",
    hidden: true,
  },
  operation: { perform, canPaginate: true },
};
