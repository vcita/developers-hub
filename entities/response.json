{
    "type": "object",
    "properties": {
        "success": {
            "type": "boolean"
        },
        "data": {
            "type": "object"
        }
    },
    "required": [
        "success",
        "data"
    ],
    "description": "Generic wrapper object for API responses, containing a success flag and a data payload."
}