---
endpoint: GET /v3/operators/op_roles/{uid}
domain: platform_administration
tags: [operators]
status: skip
savedAt: 2026-01-29T22:10:36.971Z
verifiedAt: 2026-01-29T22:10:36.971Z
timesReused: 0
skipReason: Endpoint path /v3/operators/op_roles is not routed/available in this environment (404 on list and retrieve). Cannot resolve a valid op_role uid to complete the test.
---
# Get Op roles

## Summary
User-approved skip: Endpoint path /v3/operators/op_roles is not routed/available in this environment (404 on list and retrieve). Cannot resolve a valid op_role uid to complete the test.

## ⚠️ Skip Reason

**This endpoint should be SKIPPED in automated testing.**

Endpoint path /v3/operators/op_roles is not routed/available in this environment (404 on list and retrieve). Cannot resolve a valid op_role uid to complete the test.

This is typically due to a business constraint where the endpoint works correctly but cannot be tested repeatedly (e.g., one-time operations, unique constraints).

## Prerequisites
No specific prerequisites documented.

## How to Resolve Parameters
Parameters were resolved automatically.

## Critical Learnings

No specific learnings documented.

## Request Template

Use this template with dynamically resolved UIDs:

```json
{
  "method": "GET",
  "path": "/v3/operators/op_roles/{uid}"
}
```