---
endpoint: GET /platform/v1/businesses/{business_uid}/recurly_data
domain: platform_administration
tags: [businesses, recurly-data]
status: skip
savedAt: 2026-01-30T07:24:35.659Z
verifiedAt: 2026-01-30T07:24:35.659Z
timesReused: 0
skipReason: Endpoint is intentionally internal-only and requires a specific recurlybilling app OAuth token that is not available to the test runner; cannot obtain/impersonate the recurlybilling token via public APIs.
---
# Get Recurly data

## Summary
User-approved skip: Endpoint is intentionally internal-only and requires a specific recurlybilling app OAuth token that is not available to the test runner; cannot obtain/impersonate the recurlybilling token via public APIs.

## ⚠️ Skip Reason

**This endpoint should be SKIPPED in automated testing.**

Endpoint is intentionally internal-only and requires a specific recurlybilling app OAuth token that is not available to the test runner; cannot obtain/impersonate the recurlybilling token via public APIs.

This is typically due to a business constraint where the endpoint works correctly but cannot be tested repeatedly (e.g., one-time operations, unique constraints).

## Prerequisites
No specific prerequisites documented.

## How to Resolve Parameters
Parameters were resolved automatically.

## Critical Learnings

No specific learnings documented.

## Request Template

Use this template with dynamically resolved UIDs:

```json
{
  "method": "GET",
  "path": "/platform/v1/businesses/{business_uid}/recurly_data"
}
```