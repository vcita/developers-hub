---
endpoint: POST /v1/partners/accounts/{business_uid}/reopen
domain: platform_administration
tags: [partners, reopen]
status: skip
savedAt: 2026-01-28T15:36:16.088Z
verifiedAt: 2026-01-28T15:36:16.088Z
timesReused: 0
skipReason: Partners API routing/infrastructure issue - the vcita service that hosts this endpoint is not accessible through the current API gateway, and requires special directory-scoped OAuth tokens that are not available in the test environment
---
# Create Reopen

## Summary
User-approved skip: Partners API routing/infrastructure issue - the vcita service that hosts this endpoint is not accessible through the current API gateway, and requires special directory-scoped OAuth tokens that are not available in the test environment

## ⚠️ Skip Reason

**This endpoint should be SKIPPED in automated testing.**

Partners API routing/infrastructure issue - the vcita service that hosts this endpoint is not accessible through the current API gateway, and requires special directory-scoped OAuth tokens that are not available in the test environment

This is typically due to a business constraint where the endpoint works correctly but cannot be tested repeatedly (e.g., one-time operations, unique constraints).

## Prerequisites
No specific prerequisites documented.

## How to Resolve Parameters
Parameters were resolved automatically.

## Critical Learnings

No specific learnings documented.

## Request Template

Use this template with dynamically resolved UIDs:

```json
{
  "method": "POST",
  "path": "/v1/partners/accounts/{business_id}/reopen"
}
```